# Thread

Swing on a vine, let go at the right moment, fly through the gap.
One input: tap. Canvas 2D, no dependencies, no build step.

## Install on iPhone
1. Open the GitHub Pages URL in Safari
2. Share -> Add to Home Screen
3. Fullscreen, portrait, works offline

## Design notes
Pace is set by scaling time: gravity is G/k^2 and speed is v/k with k=1.3,
which leaves every trajectory geometrically identical but traversed 30%
slower. Quarter swing is 1.16s.

Difficulty ramps over the first 45 gaps: opening 250px -> 80px, trunk
spacing 480px -> 400px, back-to-back walls appear, vine gives way sooner.
Measured release window: 574ms -> 214ms.

Levels are generated by simulating a real release, so every gap is
threadable by construction.
